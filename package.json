{
  "name": "nini_pijamas",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace frontend run dev\" \"npm --workspace backend run dev\"",
    "dev:frontend": "set WATCHPACK_POLLING=true&& set CHOKIDAR_USEPOLLING=true&& npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "typecheck": "npm --workspace frontend run typecheck && npm --workspace backend run typecheck",
    "prisma:generate": "npm --workspace backend run prisma:generate",
    "migrate": "npm --workspace backend run migrate",
    "seed": "npm --workspace backend run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
